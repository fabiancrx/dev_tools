import 'package:flutter/material.dart';

class UnixTimestampScreen extends StatelessWidget {
  const UnixTimestampScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
